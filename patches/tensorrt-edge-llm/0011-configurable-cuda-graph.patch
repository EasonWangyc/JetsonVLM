diff --git a/experimental/server/engine.py b/experimental/server/engine.py
--- a/experimental/server/engine.py
+++ b/experimental/server/engine.py
@@ -549,7 +549,11 @@ class LLM:
             self._runtime = self._rt.LLMRuntime(
                 self._engine_dir,
                 self._visual_engine_dir,
                 {},
             )
-        self._runtime.capture_decoding_cuda_graph()
+        if os.environ.get("EDGELLM_DISABLE_CUDA_GRAPH") == "1":
+            logger.info(
+                "CUDA graph capture disabled by EDGELLM_DISABLE_CUDA_GRAPH=1."
+            )
+        else:
+            self._runtime.capture_decoding_cuda_graph()
         logger.info("Engine loaded and ready.")
