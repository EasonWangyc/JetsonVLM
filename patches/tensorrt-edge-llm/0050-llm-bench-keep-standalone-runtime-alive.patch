diff --git a/examples/utils/benchLogger.cpp b/examples/utils/benchLogger.cpp
--- a/examples/utils/benchLogger.cpp
+++ b/examples/utils/benchLogger.cpp
@@ -162,9 +162,10 @@ std::pair<double, double> computeStats(std::vector<double> const& values)
 // ==================== Engine Metadata ====================
 
-std::unique_ptr<nvinfer1::ICudaEngine> loadStandaloneEngine(std::filesystem::path const& enginePath)
+std::unique_ptr<nvinfer1::ICudaEngine> loadStandaloneEngine(std::filesystem::path const& enginePath,
+    std::unique_ptr<nvinfer1::IRuntime>& runtime)
 {
-    auto runtime = std::unique_ptr<nvinfer1::IRuntime>(nvinfer1::createInferRuntime(gLogger));
+    runtime = std::unique_ptr<nvinfer1::IRuntime>(nvinfer1::createInferRuntime(gLogger));
     return deserializeCudaEngineFromFile(*runtime, enginePath);
 }
 
diff --git a/examples/utils/benchLogger.h b/examples/utils/benchLogger.h
--- a/examples/utils/benchLogger.h
+++ b/examples/utils/benchLogger.h
@@ -154,7 +154,8 @@ std::pair<double, double> computeStats(std::vector<double> const& values);
 // ==================== Engine Metadata ====================
 
 //! Load a standalone engine from file for dtype queries and layer metadata extraction
-std::unique_ptr<nvinfer1::ICudaEngine> loadStandaloneEngine(std::filesystem::path const& enginePath);
+std::unique_ptr<nvinfer1::ICudaEngine> loadStandaloneEngine(
+    std::filesystem::path const& enginePath, std::unique_ptr<nvinfer1::IRuntime>& runtime);
 
 //! Extract layer metadata from a standalone TRT engine using IEngineInspector.
 //! Parses each layer's JSON individually (typically a few KB each).
diff --git a/examples/llm/llm_bench.cpp b/examples/llm/llm_bench.cpp
--- a/examples/llm/llm_bench.cpp
+++ b/examples/llm/llm_bench.cpp
@@ -600,6 +600,7 @@ int main(int argc, char** argv)
     // Visual mode uses MultimodalRunner (unchanged from legacy)
     std::unique_ptr<rt::MultimodalRunner> visualRunner;
+    std::unique_ptr<nvinfer1::IRuntime> standaloneRuntime;
     std::unique_ptr<nvinfer1::ICudaEngine> standaloneEngine;
     int64_t imageTokens = 0;
@@ -672,7 +673,7 @@ int main(int argc, char** argv)
 
-        standaloneEngine = loadStandaloneEngine(std::filesystem::path(args.engineDir) / "visual.engine");
+        standaloneEngine = loadStandaloneEngine(std::filesystem::path(args.engineDir) / "visual.engine", standaloneRuntime);
     }
@@ -818,7 +819,7 @@ int main(int argc, char** argv)
         // --- Standalone engine for layer metadata extraction ---
-        standaloneEngine = loadStandaloneEngine(enginePath);
+        standaloneEngine = loadStandaloneEngine(enginePath, standaloneRuntime);
 
         // --- Fill PipelineIO tensors with random data ---
