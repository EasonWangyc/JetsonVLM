diff --git a/cpp/sampler/sampling.h b/cpp/sampler/sampling.h
--- a/cpp/sampler/sampling.h
+++ b/cpp/sampler/sampling.h
@@ -171,2 +171,2 @@
 void selectAllTopK(rt::Tensor const& input, rt::OptionalOutputTensor topKValues, rt::Tensor& topKIndices, int32_t topK,
-    rt::Tensor& workspace, cudaStream_t stream);
+    rt::Tensor& workspace, cudaStream_t stream, rt::Tensor const* reducedVocabMapping = nullptr);

diff --git a/cpp/sampler/sampling.cu b/cpp/sampler/sampling.cu
--- a/cpp/sampler/sampling.cu
+++ b/cpp/sampler/sampling.cu
@@ -318,4 +318,5 @@
 template <int BLOCK_SIZE>
 __global__ void greedyTop1Kernel(
-    float const* __restrict__ input, int32_t* __restrict__ output, int32_t batchSize, int32_t vocabSize)
+    float const* __restrict__ input, int32_t* __restrict__ output, int32_t batchSize, int32_t vocabSize,
+    int32_t const* __restrict__ reducedVocabMapping)
 {
@@ -338,6 +339,7 @@
     TopK_2 total = BlockReduce(tempStorage).Reduce(partial, topk2MaxOpFunctor());
     if (threadIdx.x == 0)
     {
-        output[batchIdx] = total.index >= 0 ? total.index : vocabSize - 1;
+        int32_t const reducedIndex = total.index >= 0 ? total.index : vocabSize - 1;
+        output[batchIdx] = reducedVocabMapping != nullptr ? reducedVocabMapping[reducedIndex] : reducedIndex;
     }
 }
@@ -952,4 +955,4 @@
 
 void selectAllTopK(rt::Tensor const& input, rt::OptionalOutputTensor topKValues, rt::Tensor& topKIndices, int32_t topK,
-    rt::Tensor& workspace, cudaStream_t stream)
+    rt::Tensor& workspace, cudaStream_t stream, rt::Tensor const* reducedVocabMapping)
 {
@@ -963,2 +966,12 @@
         "Invalid tensor data types");
 
+    if (reducedVocabMapping != nullptr)
+    {
+        check::check(reducedVocabMapping->getDeviceType() == rt::DeviceType::kGPU
+                && reducedVocabMapping->getDataType() == nvinfer1::DataType::kINT32,
+            "Reduced vocabulary mapping must be a GPU INT32 tensor");
+        auto const mappingShape = reducedVocabMapping->getShape();
+        check::check(mappingShape.getNumDims() == 1 && mappingShape[0] == input.getShape()[1],
+            "Reduced vocabulary mapping shape must match the logits vocabulary size");
+    }
+
@@ -993,5 +1006,6 @@
         constexpr int32_t GREEDY_BLOCK_SIZE = 256;
         greedyTop1Kernel<GREEDY_BLOCK_SIZE><<<batchSize, GREEDY_BLOCK_SIZE, 0, stream>>>(
-            input.dataPointer<float>(), topKIndices.dataPointer<int32_t>(), batchSize, vocabSize);
+            input.dataPointer<float>(), topKIndices.dataPointer<int32_t>(), batchSize, vocabSize,
+            reducedVocabMapping != nullptr ? reducedVocabMapping->dataPointer<int32_t>() : nullptr);
         return;
     }

diff --git a/cpp/runtime/decoding/vanillaDecoder.cpp b/cpp/runtime/decoding/vanillaDecoder.cpp
--- a/cpp/runtime/decoding/vanillaDecoder.cpp
+++ b/cpp/runtime/decoding/vanillaDecoder.cpp
@@ -112,18 +112,18 @@
     if (shouldUseNonGreedySampling(context.temperature, context.topK, context.topP))
     {
         SamplingParams params(activeBatchSize, mRuntime.deployment.base.outputVocabSize, context.temperature,
             static_cast<int32_t>(context.topK), context.topP);
         topKtopPSamplingFromLogits(mRuntime.base.pipelineIO.outputLogits, mRuntime.sampling.indices, params,
             mRuntime.sampling.workspace, context.stream);
+        if (mRuntime.deployment.base.reducedVocabSize > 0)
+        {
+            mapReducedVocabToFullVocab(mRuntime.sampling.indices, mRuntime.sampling.baseVocabMappingTable, context.stream);
+        }
     }
     else
     {
         constexpr int32_t kSAMPLING_TOP_K = 1;
         selectAllTopK(mRuntime.base.pipelineIO.outputLogits, std::nullopt, mRuntime.sampling.indices, kSAMPLING_TOP_K,
-            mRuntime.sampling.workspace, context.stream);
-    }
-
-    if (mRuntime.deployment.base.reducedVocabSize > 0)
-    {
-        mapReducedVocabToFullVocab(mRuntime.sampling.indices, mRuntime.sampling.baseVocabMappingTable, context.stream);
+            mRuntime.sampling.workspace, context.stream,
+            mRuntime.deployment.base.reducedVocabSize > 0 ? &mRuntime.sampling.baseVocabMappingTable : nullptr);
     }

diff --git a/cpp/runtime/llmInferenceRuntime.cpp b/cpp/runtime/llmInferenceRuntime.cpp
--- a/cpp/runtime/llmInferenceRuntime.cpp
+++ b/cpp/runtime/llmInferenceRuntime.cpp
@@ -1126,18 +1126,17 @@
     if (shouldUseNonGreedySampling(context.temperature, context.topK, context.topP))
     {
         SamplingParams params(activeBatchSize, mDeployment.base.outputVocabSize, context.temperature,
             static_cast<int32_t>(context.topK), context.topP);
         topKtopPSamplingFromLogits(
             mPipelineIO->outputLogits, mSamplingIndices, params, mSamplingWorkspace, context.stream);
+        if (mDeployment.base.reducedVocabSize > 0)
+        {
+            mapReducedVocabToFullVocab(mSamplingIndices, mBaseVocabMappingTable, context.stream);
+        }
     }
     else
     {
         constexpr int32_t kSAMPLING_TOP_K = 1;
-        selectAllTopK(mPipelineIO->outputLogits, std::nullopt, mSamplingIndices, kSAMPLING_TOP_K, mSamplingWorkspace,
-            context.stream);
-    }
+        selectAllTopK(mPipelineIO->outputLogits, std::nullopt, mSamplingIndices, kSAMPLING_TOP_K, mSamplingWorkspace,
+            context.stream, mDeployment.base.reducedVocabSize > 0 ? &mBaseVocabMappingTable : nullptr);

-    // Apply vocabulary mapping if base model uses reduced vocabulary.
-    if (mDeployment.base.reducedVocabSize > 0)
-    {
-        mapReducedVocabToFullVocab(mSamplingIndices, mBaseVocabMappingTable, context.stream);
     }
