diff --git a/cpp/runtime/llmInferenceRuntime.cpp b/cpp/runtime/llmInferenceRuntime.cpp
--- a/cpp/runtime/llmInferenceRuntime.cpp
+++ b/cpp/runtime/llmInferenceRuntime.cpp
@@ -1941,9 +1941,13 @@ bool LLMInferenceRuntime::performBatchEvict(DecodingInferenceContext& context, DecodingStrategy& strategy)
     // Upload batch mapping to GPU
-    check::check(mDeviceBatchMapping.reshape({oldActiveBatch}), "Tensor reshape failed");
-    CUDA_CHECK(cudaMemcpyAsync(mDeviceBatchMapping.rawPointer(), batchMapping.data(), oldActiveBatch * sizeof(int32_t),
-        cudaMemcpyHostToDevice, context.stream));
+    // No fixed v0.9.1 strategy consumes the device mapping when every slot is
+    // evicted; 0033 also skips all base/draft cache compaction in that case.
+    if (newActiveBatch > 0)
+    {
+        check::check(mDeviceBatchMapping.reshape({oldActiveBatch}), "Tensor reshape failed");
+        CUDA_CHECK(cudaMemcpyAsync(mDeviceBatchMapping.rawPointer(), batchMapping.data(), oldActiveBatch * sizeof(int32_t),
+            cudaMemcpyHostToDevice, context.stream));
+    }
@@ -1971,7 +1975,10 @@ bool LLMInferenceRuntime::performBatchEvict(DecodingInferenceContext& context, DecodingStrategy& strategy)
     strategy.onBatchEvict(batchMapping, oldActiveBatch, newActiveBatch, mDeviceBatchMapping, context.stream);
 
     // Compact CPU context
-    CUDA_CHECK(cudaStreamSynchronize(context.stream));
+    if (newActiveBatch > 0)
+    {
+        CUDA_CHECK(cudaStreamSynchronize(context.stream));
+    }
 
     // Save evicted batches' results before compacting (using original batch index)
