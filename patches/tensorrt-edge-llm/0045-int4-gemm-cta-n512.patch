diff --git a/cpp/kernels/int4GroupwiseGemmKernels/int4GroupwiseGemm.h b/cpp/kernels/int4GroupwiseGemmKernels/int4GroupwiseGemm.h
--- a/cpp/kernels/int4GroupwiseGemmKernels/int4GroupwiseGemm.h
+++ b/cpp/kernels/int4GroupwiseGemmKernels/int4GroupwiseGemm.h
@@ -24,5 +24,5 @@ namespace kernel
 // Authoritative tile/template constraints for gemm_forward_cuda_new and gemv_forward_cuda_new.
 // The plugin dispatcher (Int4GroupwiseGemmPlugin::enqueue) routes between the two kernels based
 // on M and N alignment; keep these names referenced on both sides instead of hard-coding.
-constexpr int32_t kGemmCtaN = 128; // gemm_forward_cuda_new requires N % kGemmCtaN == 0
+constexpr int32_t kGemmCtaN = 512; // 0045 candidate: gemm_forward_cuda_new requires N % kGemmCtaN == 0
 constexpr int32_t kGemvMaxM = 6;   // gemv_forward_cuda_new has template instantiations for M=1..6

diff --git a/cpp/plugins/int4GroupwiseGemmPlugin/int4GroupwiseGemmPlugin.cpp b/cpp/plugins/int4GroupwiseGemmPlugin/int4GroupwiseGemmPlugin.cpp
--- a/cpp/plugins/int4GroupwiseGemmPlugin/int4GroupwiseGemmPlugin.cpp
+++ b/cpp/plugins/int4GroupwiseGemmPlugin/int4GroupwiseGemmPlugin.cpp
@@ -252,7 +252,7 @@ int32_t Int4GroupwiseGemmPlugin::enqueue(PluginTensorDesc const* inputDesc,
         half* ScaleInPtr = reinterpret_cast<half*>(const_cast<void*>(inputs[2]));
         half* gemmOutDevicePtr = reinterpret_cast<half*>(outputs[0]);

-        // gemm_forward_cuda_new requires N divisible by its CTA_N tile size (128).
+        // gemm_forward_cuda_new requires N divisible by kernel::kGemmCtaN.
         // Smaller / non-aligned N (e.g. GDN's in_proj_a / in_proj_b = num_recurrent_heads
         // = 32 in Qwen3.5-4B) must use the GEMV kernel. GEMV only has template
         // instantiations for M=1..6, so split M into <=6-row chunks.
